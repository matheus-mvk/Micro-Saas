# Realtime

Socket.IO is prepared for tenant-scoped progress and notifications.

Current foundation includes a gateway and tenant room naming. Real authorization of room joins must be implemented with the auth module before production use.
