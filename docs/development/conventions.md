# Conventions

- API routes use `/api/v1`.
- Dates use UTC and ISO 8601.
- Errors follow the shared structured contract.
- Frontend consumes API responses through the HTTP client.
- Shared package contains stable enums and contracts only.
