# Security Review

Input: changed routes, data model, jobs, uploads, realtime or auth code.

Steps: check public exposure, tenant isolation, RBAC, secrets, logs, abuse cases, error messages and tests.

Output: findings fixed or explicitly accepted.
