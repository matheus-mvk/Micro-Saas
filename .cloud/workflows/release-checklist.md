# Release Checklist

Input: candidate release.

Steps: install, generate Prisma, lint, typecheck, tests, build, Compose config, Compose build, Compose up, health checks and final security review.

Output: release report with implemented, prepared, planned and pending items.
