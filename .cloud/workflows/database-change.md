# Database Change

Input: data requirement and tenant classification.

Steps: update Prisma schema, create migration, review indexes and constraints, update seed if useful, run generate and migration validation.

Output: database change with rollback notes and docs.
