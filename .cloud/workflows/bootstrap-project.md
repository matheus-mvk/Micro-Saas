# Bootstrap Project

Input: empty or partial repository.

Steps: inspect workspace, create monorepo, add shared contracts, add API/web foundations, configure Docker, add docs, run validations and record blockers.

Output: compilable foundation or explicit blocker report.
