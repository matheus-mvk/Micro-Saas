# Implement Endpoint

Input: approved API contract.

Steps: add controller route, DTO validation, guards, use case call, presenter, Swagger metadata, integration tests and error cases.

Output: endpoint documented and validated.
