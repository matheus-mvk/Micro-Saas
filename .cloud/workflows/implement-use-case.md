# Implement Use Case

Input: module skill with approved use case.

Steps: add DTOs, use case, repository method, presenter, unit tests and tenant checks.

Output: use case merged with tests and docs updated.
